<div class="form-group w-100">
    <label for="{{ $label }}">{{ $label }}</label>
    <input
        type="text"
        class="form-control w-100"
        id="{{ $label }}"
        name="{{ $attribute }}"
        value="{{ $value }}"
        readonly
    >
</div>

