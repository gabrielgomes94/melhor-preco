<div class="form-group w-100">
    <label for="">{{ $label  }} (%)</label>
    <input
        type="text"
        class="form-control w-100 input-percentage"
        id="{{ $id  }}"
        name="{{ $name }}"
        value="{{ $value }}"
    >
</div>

